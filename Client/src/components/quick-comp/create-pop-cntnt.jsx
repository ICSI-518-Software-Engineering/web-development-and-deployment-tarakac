import { DialogDescription, DialogHeader, DialogTitle } from "../ui/dialog";

export const CreatePopHeader = () => {
  return (
    <DialogHeader>
      <DialogTitle>Add Item</DialogTitle>
      <DialogDescription>Add a new item to your inventory.</DialogDescription>
    </DialogHeader>
  );
};
